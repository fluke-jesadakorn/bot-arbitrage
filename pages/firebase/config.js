import React from 'react' // Import for build only
export const firebaseConfig = process.env.firebaseConfig;
const Null = () => {
    return (<></>) // When build need to Default export
}
export default Null